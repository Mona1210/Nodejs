import { PrepUpperPipe } from './prep-upper.pipe';

describe('PrepUpperPipe', () => {
  it('create an instance', () => {
    const pipe = new PrepUpperPipe();
    expect(pipe).toBeTruthy();
  });
});
